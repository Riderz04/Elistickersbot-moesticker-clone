version: "3.9"

services:
  moe-sticker-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: moe-sticker-bot
    restart: unless-stopped
    env_file: .env

    volumes:
      - ./data:/app/data
      - ./logs:/app/logs

    ports:
      - "8080:8080"
